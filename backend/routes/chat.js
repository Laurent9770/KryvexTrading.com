const express = require('express');
const router = express.Router();

// Placeholder - will be implemented with chat features
router.get('/', (req, res) => {
  res.json({ message: 'Chat route - coming soon' });
});

module.exports = router; 