const express = require('express');
const router = express.Router();

// Placeholder - will be implemented with admin features
router.get('/', (req, res) => {
  res.json({ message: 'Admin route - coming soon' });
});

module.exports = router; 