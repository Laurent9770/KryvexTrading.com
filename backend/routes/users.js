const express = require('express');
const router = express.Router();

// Placeholder - will be implemented with user management features
router.get('/', (req, res) => {
  res.json({ message: 'Users route - coming soon' });
});

module.exports = router; 