const express = require('express');
const router = express.Router();

// Placeholder - will be implemented with KYC features
router.get('/', (req, res) => {
  res.json({ message: 'KYC route - coming soon' });
});

module.exports = router; 