const express = require('express');
const router = express.Router();

// Placeholder - will be implemented with trading features
router.get('/', (req, res) => {
  res.json({ message: 'Trading route - coming soon' });
});

module.exports = router; 