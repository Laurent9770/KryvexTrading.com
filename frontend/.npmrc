# Use npm instead of Bun for better compatibility
package-manager=npm
# Force installation of all dependencies including optional ones
optional=true
# Ensure all dependencies are installed
include=optional 